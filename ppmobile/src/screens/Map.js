import React from 'react';
import { View } from 'react-native';
import MapView from 'react-native-maps';

const Map = (props) => {
  return (
    <View style={{ flex: 2}}/>
  );
};

export default Map;